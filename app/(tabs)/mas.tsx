// Pantalla placeholder para el tab "Más"
// La navegación real se maneja desde MasTabButton que abre el menú inferior
import { View } from 'react-native';

export default function MasScreen() {
    return <View />;
}
